<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define log directory for macOS -->
    <property name="LOGS" value="/Users/khanhle/Documents/udemy_course/tom-spring/logs" />

    <!-- Console appender -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%green(%d{yyyy-MM-dd HH:mm:ss.SSS}) %blue(%-5level) %magenta(${PID:-}) --- %red([%15.15thread]) %yellow(%-40.40logger{39}): %msg%n%throwable</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/tom-spring-logger.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %-40.40logger{39}: %msg%n%throwable</pattern>
        </encoder>

        <!-- Use SizeAndTimeBasedRollingPolicy directly -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/archived/tom-spring-logger-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Debug level group -->
    <logger name="org.tommap.tomlearnspring.eazy_school.aspects" level="DEBUG" additivity="false">
        <appender-ref ref="RollingFile" />
        <appender-ref ref="Console" />
    </logger>

    <logger name="org.tommap.tomlearnspring.eazy_school.controller.ContactController" level="DEBUG" additivity="false">
        <appender-ref ref="RollingFile" />
        <appender-ref ref="Console" />
    </logger>

    <!-- Trace level group -->
    <logger name="org.tommap.tomlearnspring.eazy_school.controller.DashboardController" level="TRACE" additivity="false">
        <appender-ref ref="RollingFile" />
        <appender-ref ref="Console" />
    </logger>

    <!-- LOG everything at INFO level -->
    <root level="INFO">
        <appender-ref ref="RollingFile" />
        <appender-ref ref="Console" />
    </root>

</configuration>